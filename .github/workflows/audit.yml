name: Line Item Inspector

on:
  pull_request:
    types: [labeled]

jobs:
  audit:
    if: github.event.label.name == 'audit-requested'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Hello World
        run: echo "Audit workflow triggered"